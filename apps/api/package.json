{
  "name": "@viral-radar/api",
  "version": "0.0.4",
  "private": true,
  "type": "module",
  "description": "Collection pipeline, trend engine and HTTP API. Zero runtime dependencies.",
  "main": "src/main.ts",
  "bin": {
    "radar": "./src/main.ts"
  },
  "scripts": {
    "start": "node src/main.ts serve",
    "test": "node --test tests/*.test.ts",
    "typecheck": "tsc --noEmit"
  }
}
